import { INestApplication } from '@nestjs/common';
// import * as express from 'express';
// import * as path from 'path';

export function setUpUploadRoutes(app: INestApplication) {
  // app.use('/uploads/courses', express.static(path.resolve(`../../${PATH_UPLOAD_COURSE}`)));
  // app.use('/uploads/videos', express.static(path.resolve(`../../${PATH_UPLOAD_LESSON}`)));
}
